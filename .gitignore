# Machine-specific Claude Code permissions, not shared tooling
.claude/settings.local.json

# Local-only console helper for previewing the admin panel without Firestore.
preview-admin.js

# Built by scripts/build-admin.js at deploy time.
admin-dist/

# Firebase CLI deploy cache.
.firebase/

# Built by scripts/build-app.js at deploy time.
app-dist/
